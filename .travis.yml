sudo: false
language: node_js
node_js:
  - "8"
  - "6"
  - "4"
  - "node"
script:
  - "npm run build"
# after_script:
#  - "npm install coveralls@2.11.x && cat coverage/lcov.info | coveralls"
matrix:
  fast_finish: true